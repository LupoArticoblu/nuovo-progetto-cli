import { createApp } from 'vue'
//import './style.css' <- per convenzione si elimina questo e si usa un file nominato General.scss che avrà tutti i nostri stili globali
import App from './App.vue'

createApp(App).mount('#app')
